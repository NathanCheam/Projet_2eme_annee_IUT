package main.problem.constraint;

/**
 * The MemoryLimit class implements the IContrainte interface to impose memory constraints on a process.
 * @author Notteau Romain
 */
public class MemoryLimit implements IContrainte {

    /**
     * Runs the memory constraint on the given process.
     *
     * @param process The process on which the memory constraint is to be applied.
     */
    @Override
    public void run(Process process) {
        // Not implemented yet...
    }
}